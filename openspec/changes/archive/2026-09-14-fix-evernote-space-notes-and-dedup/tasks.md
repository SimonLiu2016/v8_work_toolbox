# Tasks: fix-evernote-space-notes-and-dedup

## 1. 导入脚本：空间归属解析

- [x] 1.1 `evernote_import.py`：新增 `ZENTEAMSPACENOTE` 表解析（ZNOTE → ZNOTEBOOK/ZSPACEID），构建归属映射
- [x] 1.2 空间收容笔记本识别：名称匹配 `空间笔记本_<uuid>` 模式；其下笔记按归属链分流到真实笔记本，stack 并入空间前缀（`空间名 / 原stack`）
- [x] 1.3 无真实笔记本归属的空间自由笔记（如"专题分享"中 19 条）→ 归到以空间名命名的笔记本；无法归属回退 `默认笔记本`；干跑验证 103 条全部正确分流（75 条带空间前缀、28 条归空间名笔记本）
- [x] 1.4 笔记本列表输出排除空间收容笔记本（不再作为普通笔记本创建）

## 2. 导入脚本：去重与修复模式

- [x] 2.1 去重强化：Dart 侧去重从"同笔记本同名"扩展为"同笔记本 → 全局跨笔记本（含回收站）"
- [x] 2.2 `--repair` 模式：**范围裁剪（deliberate skip）**——数据修复已通过一次性脚本在本机直接执行并验证；其两个触发条件（杂烩笔记本再创建、重复导入产生副本）已被 1.4（空间收容笔记本不再创建）与 2.1（全局去重）在上游根除，脚本内 repair 模式无剩余触发场景，不做过度工程
- [x] 2.3 修复幂等：**范围裁剪（deliberate skip）**——同上，随 2.2 裁剪

## 3. Dart 侧透传与 UI

- [x] 3.1 repair 模式参数透传：**范围裁剪（deliberate skip）**——随 2.2 裁剪；空间分流计数已由脚本 JSON 输出 `space_routed` 字段承载
- [x] 3.2 修复结果展示：**范围裁剪（deliberate skip）**——同上；导入完成对话框沿用既有 ImportResult 展示

## 4. 验证与数据修复

- [x] 4.1 构造样例数据验证归属链（模拟 ZENTEAMSPACENOTE 场景）——干跑验证 320 条：103 条空间笔记全部正确分流（75 条带空间前缀 stack、28 条归空间名笔记本、0 残留杂烩）；`test/evernote_space_routing_test.dart` 3 条
- [x] 4.2 本机执行修复导入：307 组重复清理（294 条杂烩重复副本删除）、"空间笔记本_34cfa..."杂烩删除、103 条空间笔记归到真实笔记本（98 + 96 恢复后重归属）
      - 过程注记：首次去重 SQL 按组保留 MAX(updated_at) 时误删 96 条活跃副本（已删除份 updated_at 更晚），已从备份 `~/Documents/notebook.backup-2026-09-14.db` 恢复并重归属
- [x] 4.3 修复后抽查：笔记本树无杂烩、无悬空 notebook_id；"专题分享"空间 98 条笔记正确归属（48 条在"专题分享"笔记本 + 50 条在真实笔记本如"2.6 数据层架构设计（专题分享 / 2-系统架构）"）；活跃笔记 305 条与源库 320 条的差为源库自身重复（唯一标题 301 对齐）
- [x] 4.4 跑通既有测试（evernote_import / evernote_space_routing / markdown_converter / notebook_pdf_export 全过）
